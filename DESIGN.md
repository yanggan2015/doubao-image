# 设计设想（Design Intent）— 豆包版

与元宝 skill 同一套产品思路，换成 **豆包网页**（`doubao.com`）落地。

## 核心设想

1. **信任提示词，少点 UI**  
   不选手动比例/分辨率菜单；「四张、草稿、>1080P、不要4K」写进输入框。自动化只做：Chrome CDP → 登录 → 发两次字 → 等图 → 下载。

2. **两阶段**  
   - **第1轮 = 快 / 发散**：同一描述快速出四张草稿，供 **4 选 1**。  
   - **第2轮 = 收敛 / 质量**：让豆包自选最贴合的一张并优化重绘，目标 **大于 1080P**，**不要 4K**。

3. **豆包选图，脚本落地**  
   选图与画质交给模型；脚本负责登录态、等待 DOM、带 Cookie 下载。

4. **入口**  
   默认打开 `https://www.doubao.com/chat/create-image`（AI 创作 / 图像）。发送后常跳到对话页，第2轮在同一会话继续即可。

5. **可带走**  
   自包含 skill：clone → `setup.sh` → 登录一次 → `run.sh`。数据默认在本目录（`.venv` / `.chrome-profile` / `downloads`，gitignore）。CDP 默认端口 **9334**（与元宝 9333 并存）。

## 成功标准

- 用户只给一句画面描述。  
- 得到 `downloads/.../final/` 下一张优化图（尽量 >1080P）。  
- 换机可复现（需本机 Chrome + 可登录豆包）。
